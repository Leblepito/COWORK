# Nixpacks fallback config (used if railway.toml builder != dockerfile)
# Primary deployment uses Dockerfile via railway.toml
providers = ["node"]

[phases.install]
cmds = ["cd frontend && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "cd frontend && npm run start"
